[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lendkit-shared"
version = "0.1.0"
description = "LendKit shared utilities — events, middleware, validators"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["."]
